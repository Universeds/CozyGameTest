--!strict

return {
	batchSize = 50, -- Number of component updates per batch
	batchInterval = 0.1, -- Seconds between batch sends
	
	useSquash = true,
	
	remoteNamespace = "Replication",
	
	debugMode = true,
}
